package com.example.quizapp.ui.auth.base

import androidx.lifecycle.ViewModel

// error State/SharedFlow (e.g. incorrect password, user already exists)
// verify all fields filled
abstract class BaseAuthViewModel : ViewModel() {
}